---
phase: quick-260822-01
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - smartpos-main/lib/store.ts
  - smartpos-main/app/features/customer-mode/CustomerMode.tsx
autonomous: true
requirements: [QUICK-260822-01]
must_haves:
  truths:
    - "Attendant can send the current cart to the kitchen as a real order (status 'pending') WITHOUT any payment; cart clears after send"
    - "Kitchen display shows sent orders immediately (they are ordinary 'pending' rows)"
    - "Header 'Settle Now' enters settlement mode instead of silently marking orders settled"
    - "In settlement mode, Place & Pay is enabled iff (activeOrders OR cart has items) and charges activeOrdersTotal + cart total-with-tax ONCE"
    - "On payment success, ALL pending/in_progress orders of table+tab become 'settled' WITH payment_method and razorpay ids stamped"
    - "A combined receipt shows every item from every settled order plus cart extras, with summed subtotal/tax/total"
    - "Changing table or tab exits settlement mode"
    - "Zero cart AND zero active orders => Place & Pay stays disabled"
  artifacts:
    - path: "smartpos-main/lib/store.ts"
      provides: "settleOrdersWithPayment action stamping payment fields"
      contains: "settleOrdersWithPayment"
    - path: "smartpos-main/app/features/customer-mode/CustomerMode.tsx"
      provides: "Send-to-Kitchen button, settle mode, combined settlement payment flow, combined receipt"
      contains: "unpaid"
  key_links:
    - from: "smartpos-main/app/features/customer-mode/CustomerMode.tsx"
      to: "usePOSStore.settleOrdersWithPayment"
      via: "called in handlePaymentSuccess with { method, razorpayOrderId?, razorpayPaymentId? }"
      pattern: "settleOrdersWithPayment\\("
    - from: "smartpos-main/app/features/customer-mode/CustomerMode.tsx"
      to: "usePOSStore.createOrder"
      via: "Send-to-Kitchen inserts row with payment_method 'unpaid'"
      pattern: "payment_method: \"unpaid\""
    - from: "smartpos-main/app/features/customer-mode/CustomerMode.tsx"
      to: "PaymentModal amount prop"
      via: "combined grand total passed once"
      pattern: "amount=\\{activeOrdersTotal \\+ totalWithTax\\}"
---

<objective>
Implement the KOT-style flow in CustomerMode: send carts to the kitchen as real UNPAID orders, then settle ALL combined unpaid orders + current cart in ONE payment, marking every order 'settled' with payment data stamped and showing a single combined receipt.

Purpose: closes the revenue-integrity hole where header "Settle All" wiped tabs with no payment record; delivers the roadmap vision (send-now, pay-later, settle-once).
Output: modified `smartpos-main/lib/store.ts` (one new action) and `smartpos-main/app/features/customer-mode/CustomerMode.tsx` (settle mode UI + payment flow).
</objective>

<execution_context>
@C:/Users/arush/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/arush/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@smartpos-main/lib/store.ts
@smartpos-main/app/features/customer-mode/CustomerMode.tsx
@smartpos-main/lib/receipt.ts

## Verified codebase facts (trust these; line numbers from current files)

**store.ts**
- `createOrder` (:203) inserts into Supabase `orders` with status:'pending', order_type:'customer', table/tab from store state; requires `customer_name: string` and `customer_phone: string`; auto-refreshes activeOrders after insert.
- `settleTab` (:141) bulk-updates pending/in_progress rows of table+tab to 'settled' with NO payment info, then `set({ activeOrders: [], cart: [] })`. KEEP IT UNTOUCHED — RestaurantOrdering.tsx imports it (per QD-02).
- `ActiveOrder` interface (:26) has: id, items, subtotal, tax, total, status, payment_method, created_at.

**CustomerMode.tsx**
- Store destructuring block :473-490 (imports `settleTab` at :486 — remove from THIS component only).
- `handleProceedToPayment` :553-557; `handleSettleAll` :559-567 (to be replaced); `handlePaymentSuccess` :579-659.
- Memos: `cartTotal` :543, `taxAmount` (18% of cart only) :544, `totalWithTax` :545, `activeOrdersTotal` :661, `activeOrdersTax` :665, `activeOrdersSubtotal` :669.
- CartContent shared footer :754-793 — rendered in BOTH desktop sidebar and mobile drawer. "Place & Pay" button :779-785 (disabled={cart.length === 0}).
- Table select onChange :818, Tab select onChange :833 (call store setters directly today).
- Header "Settle All" button :844-850.
- `<PaymentModal amount={totalWithTax} ...>` :956-965.
- Existing cash-payment id convention in handlePaymentSuccess :600-603: `cash_${Date.now()}` / `cash_payment_${Date.now()}`.

**lib/receipt.ts** — `ReceiptOrder` shape needed by ReceiptModal + buildReceiptText: `{ id: number; created_at: string; items[]; subtotal; tax; total; paymentMethod?: "online"|"cash"; cashReceived?; change?; currency?; restaurantName?; customerInfo? }`.

**Out of scope:** RestaurantOrdering.tsx (walk-in flow), kitchen-mode/KitchenMode.tsx (already shows all pending/in_progress orders automatically), schema changes (orders table columns suffice).

## Locked decisions (cite these tags in commits/questions)
- QD-01: Unpaid kitchen orders reuse `createOrder` with `payment_method: "unpaid"` and empty razorpay ids. No schema change.
- QD-02: New store action `settleOrdersWithPayment(restaurantId, tableNumber, tabNumber, { method, razorpayOrderId?, razorpayPaymentId? })`. Keep `settleTab` untouched.
- QD-03: Settlement amount = `activeOrdersSubtotal + activeOrdersTax + cartTotal + taxAmount` (18% tax computed on CART ONLY). Prior order rows keep their stored amounts — no retroactive edits.
- QD-04: Header "Settle All" becomes payment-first settle mode entry. Do NOT touch RestaurantOrdering.tsx.
- QD-05: `settleMode` is local CustomerMode state; reset when table or tab selection changes.
- QD-06: Combined receipt merges items from activeOrders + cart; receipt id = last created order id, or first active order id when cart was empty.
- QD-07: Keep Clear Cart as-is; provide a way back out of settle mode (toggle off).
- QD-08: Settle Now with zero cart AND zero active orders must NOT enable Place & Pay.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add settleOrdersWithPayment store action</name>
  <files>smartpos-main/lib/store.ts</files>
  <action>
Add to the `POSState` interface (after the `settleTab` declaration, ~line 54):

```
settleOrdersWithPayment: (
  restaurantId: string,
  tableNumber: number,
  tabNumber: number,
  payment: { method: string; razorpayOrderId?: string; razorpayPaymentId?: string }
) => Promise<void>;
```

Implement it in the store body directly AFTER the existing `settleTab` implementation (~line 156), mirroring settleTab's structure so the two read as siblings (per QD-02):

1. Copy settleTab's supabase update shape exactly: `.from('orders').update({...}).eq('restaurant_id', ...).eq('table_number', ...).eq('tab_number', ...).in('status', ['pending', 'in_progress'])`.
2. The update payload stamps payment data (this is the whole point — unlike settleTab): `{ status: 'settled', payment_method: payment.method, razorpay_order_id: payment.razorpayOrderId || null, razorpay_payment_id: payment.razorpayPaymentId || null, updated_at: new Date().toISOString() }`.
3. On success: `set({ activeOrders: [] })` — do NOT clear cart here (the UI clears it after building the receipt).
4. Wrap errors identically to settleTab: console.error('Error settling orders with payment:', err) and rethrow.

DO NOT modify `settleTab`, `createOrder`, or any other action. DO NOT add dependencies.
  </action>
  <verify>
    <automated>cd smartpos-main; npx tsc --noEmit; if ($?) { $c = Select-String -Path lib\store.ts -Pattern "settleOrdersWithPayment" | Measure-Object | % Count; $t = Select-String -Path lib\store.ts -Pattern "settleTab:" | Measure-Object | % Count; Write-Output "settleOrdersWithPayment refs: $c (expect >=2); settleTab decl present: $t (expect 1)" }</automated>
  </verify>
  <done>`npx tsc --noEmit` passes; `settleOrdersWithPayment` declared in interface and implemented; `settleTab` byte-identical to before.</done>
</task>

<task type="auto">
  <name>Task 2: Settle mode UI — Send to Kitchen, Settle Now toggle, gated Place & Pay</name>
  <files>smartpos-main/app/features/customer-mode/CustomerMode.tsx</files>
  <action>
All edits in CustomerMode.tsx default export. This task wires UI state and the unpaid-send path; payment-success rework is Task 3.

1. **State + store bindings (QD-05):**
   - Add `const [settleMode, setSettleMode] = useState(false);` next to the other useState calls (~line 462).
   - Add `const [sendingToKitchen, setSendingToKitchen] = useState(false);`
   - In the `usePOSStore()` destructure (~line 486): REMOVE `settleTab`, ADD `settleOrdersWithPayment`.

2. **Reset settle mode on table/tab change (QD-05):** Replace the inline `onChange={(e) => setSelectedTable(...)}` (:818) with `onChange={(e) => handleTableChange(parseInt(e.target.value))}` and tab (:833) with `handleTabChange(parseInt(e.target.value))`. Define near handleProceedToPayment:
   ```
   const handleTableChange = (t: number) => { setSettleMode(false); setSelectedTable(t, restaurantId); };
   const handleTabChange = (t: number) => { setSettleMode(false); setSelectedTab(t, restaurantId); };
   ```

3. **Replace handleSettleAll (:559-567) with a settle-mode toggle (QD-04):** Delete handleSettleAll entirely. Header button (:844-850) becomes: onClick={() => setSettleMode((s) => !s)}, label `{settleMode ? "Cancel Settlement" : "Settle Now"}`, and when settleMode is true switch classes to amber warning styling (e.g. bg-amber-500 hover:bg-amber-600) so the active state is obvious. Keep the CreditCard icon.

4. **Send to Kitchen (QD-01):** Define handler:
   ```
   const handleSendToKitchen = async () => {
     if (!restaurantId || cart.length === 0 || sendingToKitchen) return;
     setSendingToKitchen(true);
     try {
       const order = await createOrder({
         restaurant_id: restaurantId,
         razorpay_order_id: "",
         razorpay_payment_id: "",
         items: cart.map((item) => ({ name: item.name, price: item.price, quantity: item.quantity, options: item.options || [], notes: item.notes || "" })),
         subtotal: cartTotal,
         tax: taxAmount,
         total: totalWithTax,
         customer_name: "",
         customer_phone: "",
         payment_method: "unpaid",
       });
       clearCart();
       toast.success(`Order #${order.id} sent to kitchen`);
     } catch {
       toast.error("Failed to send order to kitchen");
     } finally {
       setSendingToKitchen(false);
     }
   };
   ```
   Note: createOrder auto-refreshes activeOrders, so the new order appears under Previous Orders immediately; KitchenMode polls and shows it because status is 'pending'.

5. **Cart footer buttons (CartContent, ~lines 779-785):** Between the Grand Total row and Clear Cart, render TWO stacked buttons:
   - "Send to Kitchen": full-width, indigo/slate outline style (e.g. `border border-indigo-500 text-indigo-600 py-3 rounded-lg hover:bg-indigo-50 font-medium transition-colors`), onClick={handleSendToKitchen}, `disabled={cart.length === 0 || sendingToKitchen}`, label `{sendingToKitchen ? "Sending..." : "Send to Kitchen"}`.
   - "Place & Pay" (existing button, restyled when settle mode active):
     * Compute above the JSX: `const canPay = settleMode && (activeOrders.length > 0 || cart.length > 0);` (QD-08: zero cart AND zero orders never enables).
     * onClick={() => { if (!canPay) return; setShowMobileCart(false); setShowCustomerInfo(true); }} (replaces handleProceedToPayment's guard; you may rewrite handleProceedToPayment to take this guard or inline it).
     * disabled={!canPay}.
     * When settleMode: solid green (`bg-green-600 hover:bg-green-700`) and label `Pay ${formatCurrency(activeOrdersTotal + totalWithTax, currency)} (Settle)` — wait, currency is INR; use `{settleMode ? \`Settle \${formatCurrency(activeOrdersTotal + totalWithTax, currency)}\` : "Place & Pay"}`.
   - Leave Clear Cart button untouched (QD-07).

6. **Settlement banner:** Inside CartContent top (above the empty-state/items area), when settleMode render a slim amber strip: "Settlement Mode — paying for {activeOrders.length} previous order(s) + current cart".

7. **PaymentModal amount (:958):** Change `amount={totalWithTax}` to `amount={activeOrdersTotal + totalWithTax}` (QD-03). This is safe even outside settle mode because the modal only opens when canPay.

Do NOT touch RestaurantOrdering.tsx, KitchenMode.tsx, or lib/receipt.ts.
  </action>
  <verify>
    <automated>cd smartpos-main; npx tsc --noEmit; if ($?) { rg -c "unpaid" app\features\customer-mode\CustomerMode.tsx; rg -c "handleSettleAll" app\features\customer-mode\CustomerMode.tsx; rg -c "settleTab" app\features\customer-mode\CustomerMode.tsx; rg -c "settleTab" components\dashboard\RestaurantOrdering.tsx }</automated>
  </verify>
  <done>tsc passes; "unpaid" present; handleSettleAll count 0; settleTab count 0 in CustomerMode but still >=1 in RestaurantOrdering.tsx (untouched); header button toggles settle mode; Place & Pay disabled outside settle mode.</done>
</task>

<task type="auto">
  <name>Task 3: Combined settlement payment flow + combined receipt</name>
  <files>smartpos-main/app/features/customer-mode/CustomerMode.tsx</files>
  <action>
Rework `handlePaymentSuccess` (:579-659). Keep the overall skeleton (guards, email/WhatsApp tail, catch block) but change WHAT is charged and recorded. Sequence matters (QD-06, QD-03):

1. **Snapshot BEFORE any await** (render-closure values): `const priorItems = activeOrders.flatMap((o) => o.items);` `const cartItems = cart.map(...same mapping as before...);` `const combinedItems = [...priorItems, ...cartItems];` `const grandTotal = activeOrdersTotal + totalWithTax;` `const combinedSubtotal = activeOrdersSubtotal + cartTotal;` `const combinedTax = activeOrdersTax + taxAmount;`

2. **Receipt id (QD-06):** `let receiptId: number;`

3. **If cart.length > 0:** call `createOrder` FIRST with the REAL payment (so a failed insert aborts before anything is marked settled): same orderData shape as today (:596-618) — payment_method: paymentData.method, razorpay ids using the existing cash-convention fallback (:600-603), subtotal: cartTotal, tax: taxAmount, total: totalWithTax. Assign `receiptId = order.id`. (This row is inserted already-paid; settleOrdersWithPayment will restamp identical values — harmless.)
   **If cart empty:** `receiptId = activeOrders[0].id` (first active order id per QD-06).

4. **Settle everything (QD-02):** `await settleOrdersWithPayment(restaurantId, selectedTable, selectedTab, { method: paymentData.method, razorpayOrderId: paymentData.razorpayOrderId, razorpayPaymentId: paymentData.razorpayPaymentId });` — stamps ALL pending/in_progress rows of table+tab as 'settled' with payment fields and empties activeOrders in the store.

5. **Combined receipt (QD-06):** Build a `ReceiptOrder`-shaped object:
   ```
   const receiptOrder = {
     id: receiptId,
     created_at: new Date().toISOString(),
     items: combinedItems,
     subtotal: combinedSubtotal,
     tax: combinedTax,
     total: grandTotal,
     paymentMethod: paymentData.method,
     cashReceived: paymentData.cashReceived,
     change: paymentData.change,
     currency,
     restaurantName,
     customerInfo,
   };
   ```
   (Matches lib/receipt.ts ReceiptOrder exactly — buildReceiptText and ReceiptModal consume it unchanged.)

6. **Cleanup:** setLastOrder(receiptOrder); setShowPayment(false); setShowReceipt(true); clearCart(); setCustomerInfo(null); setSettleMode(false); toast.success(`Settled ${activeOrders.length + (cart.length > 0 ? 1 : 0)} order(s) — ${formatCurrency(grandTotal, currency)}`); — compute those counts from step-1 snapshots, NOT live store state (store already mutated by then). Keep the existing sendReceiptEmail/openWhatsAppReceipt tail verbatim, fed by buildReceiptText(receiptOrder).

7. Remove nothing else: guards for missing restaurantId/customerInfo stay first in the try block.

Final manual sanity path (executor walks it mentally): cart=[X], priors=[O1,O2] -> Settle Now -> Place & Pay enabled showing O1.total+O2.total+X w/tax -> pay online 550 -> O1,O2,X all 'settled' in Supabase with method+ids -> receipt lists O1+O2+X items, subtotal sums, one total -> cart empty, settle mode off, activeOrders empty.
  </action>
  <verify>
    <automated>cd smartpos-main; npx tsc --noEmit; if ($?) { npm run lint }; if ($?) { rg -c "settleOrdersWithPayment" app\features\customer-mode\CustomerMode.tsx; rg -c "created_at: new Date\(\).toISOString\(\)" app\features\customer-mode\CustomerMode.tsx }</automated>
  </verify>
  <done>tsc + lint pass; handlePaymentSuccess creates cart order (if any) BEFORE settling, calls settleOrdersWithPayment once, shows combined ReceiptOrder-compliant receipt, clears cart and exits settle mode on success only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| POS UI -> Razorpay checkout | Payment amount crosses to third-party checkout |
| POS UI -> Supabase orders | Order status/payment writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-Q1-01 | Tampering | Client-side grand total (activeOrdersTotal + totalWithTax) | accept | Amount recomputed from DB-backed activeOrders rows on every render; single-tenant POS terminal behind auth; matches existing risk posture of current cart flow |
| T-Q1-02 | Repudiation | Unpaid orders have blank customer_name/phone | mitigate | Orders carry table_number/tab_number + created_at; settlement stamps real customer info + payment ids onto ALL rows of the tab at pay time |
| T-Q1-03 | Elevation | settleOrdersWithPayment callable with arbitrary method string | accept | Internal store action invoked only from authenticated POS screen post-PaymentModal ('online'/'cash' literals); no public endpoint exposed |
| T-Q1-SC | Tampering | npm installs | mitigate | Zero new dependencies in this plan; slopcheck N/A |
</threat_model>

<verification>
1. `cd smartpos-main && npx tsc --noEmit` — clean
2. `npm run lint` — clean
3. `rg -c "settleTab" smartpos-main/components/dashboard/RestaurantOrdering.tsx` — unchanged (>=1); file untouched
4. `rg -c "unpaid" smartpos-main/app/features/customer-mode/CustomerMode.tsx` — >=1
5. Manual smoke (dev server): add item -> Send to Kitchen -> row appears in KitchenMode with status pending, no payment collected -> repeat -> Settle Now -> Place & Pay enabled showing combined total -> cash pay -> all rows settled with payment_method='cash' + cash_ ids in Supabase -> combined receipt -> cart empty, mode exited
6. Edge: fresh table+tab, empty cart -> Settle Now -> Place & Pay remains disabled (QD-08)
7. Edge: change table mid-settlement -> settle mode resets (QD-05)
</verification>

<success_criteria>
- Attendant sends multiple unpaid orders to one tab; kitchen sees each immediately
- Single payment settles the entire tab: every prior order row gains payment_method + razorpay ids + status 'settled'
- One combined receipt listing all items from all orders with correct summed amounts (18% tax applied only to cart items; prior rows keep stored tax)
- Header button no longer silently marks orders settled
- TypeScript strict-clean, lint-clean, no new deps, no schema changes
- All work committed LOCALLY (git add/commit only — absolutely no git push)
</success_criteria>

<output>
Create `.planning/quick/260822-01-kitchen-orders-combined-settlement/260822-01-SUMMARY.md` when done
</output>
